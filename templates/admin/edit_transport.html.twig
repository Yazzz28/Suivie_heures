{% extends 'base.html.twig' %}

{% block title %}Modifier le transport{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h4 class="mb-0">Modifier la valeur du transport</h4>
                    </div>
                    <div class="">
                        {{ form_start(form) }}

                        <div class="mb-3">
                            {{ form_label(form.value, 'Valeur du transport', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.value, {'attr': {'class': 'form-control', 'placeholder': 'Entrez la valeur'}}) }}
                            {{ form_errors(form.value) }}
                        </div>

                        <div class="d-flex justify-content-between">
                            <button type="submit" class="btn btn-primary">
                                💾 Sauvegarder
                            </button>
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}